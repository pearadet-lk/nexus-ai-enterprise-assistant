apiVersion: v1
kind: Secret
metadata:
  name: nexusai-secrets
  namespace: nexusai
type: Opaque
stringData:
  # Override at deploy time: kubectl create secret ... --dry-run=client -o yaml | kubectl apply -f -
  mssql-sa-password: "Your_strong_password123"
  openai-api-key: ""
